{
  "name": "visioncreator",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npx turbo run build",
    "dev": "npx turbo run dev --parallel",
    "generate": "npx turbo run generate",
    "api:build": "npx turbo run build --filter=api",
    "api:start": "npx turbo run start --filter=api"
  },
  "devDependencies": {
    "turbo": "^2.0.4",
    "vite-plugin-tailwind-purgecss": "^0.3.3"
  },
  "engines": {
    "node": ">=20.12.2"
  },
  "packageManager": "npm@10.5.0",
  "volta": {
    "node": "20.12.2",
    "npm": "10.5.0"
  },
  "dependencies": {
    "@trigger.dev/sdk": "^2.3.19",
    "@trigger.dev/sveltekit": "^2.3.19"
  }
}